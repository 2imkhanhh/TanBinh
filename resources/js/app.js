import './bootstrap';
import { createApp } from 'vue';

const app = createApp({});

// Định nghĩa các component Vue ở đây khi bạn làm trang Admin
// app.component('example-component', ExampleComponent);

// Thay đổi '#app' thành id của phần tử root mà bạn muốn mount Vue
app.mount('#app');
